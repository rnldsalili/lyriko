{
  "name": "lyriko",
  "private": true,
  "scripts": {
    "cf-typegen": "npm run -r --parallel cf-typegen",
    "dev": "pnpm run -r --parallel --aggregate-output dev",
    "lint": "pnpm run -r --parallel lint",
    "format": "pnpm run -r --parallel format",
    "typecheck": "pnpm run -r --paralle typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "eslint": "^9.36.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "wrangler": "^4.38.0"
  }
}
